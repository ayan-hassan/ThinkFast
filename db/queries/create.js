const db = require('../connection');

const insertQuiz = function(quiz) {
  return db
    .query()
};

const insertQuestions = function(quiz) {

};

const insertOptions = function(quiz) {

};


module.exports = {getCategories}
